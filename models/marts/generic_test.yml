models:
  - name: fact_oder
    columns:
      - name: orderkey
        tests:
          - unique
          - not_null
          - relationships:
              to: ref('stg_tpch_orders')
              field: orderkey